# src/sensor_fusion/CMakeLists.txt

# Define the Fusion interface library
add_library(FusionLib INTERFACE)

# Set include directories for this library
target_include_directories(FusionLib 
    INTERFACE
)
